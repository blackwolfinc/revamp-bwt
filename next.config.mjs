// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'img.gamemonetize.com', 
      'www.blackwolftech.id', 
      'blackwolftech.id'
    ],
  },
};

  
  export default nextConfig;